[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "acg-python-sdk"
version = "1.0.0"
description = "Python SDK for Agent Commerce Gateway (Merchant Control Plane for AI Transactions)"
readme = "README.md"
authors = [{ name = "Antigravity Architecture Team" }]
license = { text = "MIT" }
dependencies = []
