apiVersion: v1
kind: Service
metadata:
  name: agent-commerce-gateway
  labels:
    app.kubernetes.io/name: agent-commerce-gateway
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 3000
      protocol: TCP
      name: http
  selector:
    app.kubernetes.io/name: agent-commerce-gateway
